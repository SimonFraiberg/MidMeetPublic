import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: { main: "#ebcbae", dark: "#582b2b" },
    secondary: { main: "#a87a51" },
  },
});

export default theme;
